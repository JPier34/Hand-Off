[build]
  command = "npm run build"
  publish = "frontend/dist"
  functions = "frontend/netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "DYNAMIC_ENVIRONMENT_ID,CHAIN_ID,MOCK"
  PNPM_FLAGS = "--no-frozen-lockfile"

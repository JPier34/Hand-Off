// HandOff.sol compiled bytecode — extracted from Hardhat artifacts
// To regenerate: cd contracts && npx hardhat compile
// Then copy .bytecode from artifacts/contracts/HandOff.sol/HandOff.json
//
// IMPORTANT: This must be updated whenever HandOff.sol changes.
// The ABI is imported separately from HandOff.abi.json.

export const HANDOFF_BYTECODE = '0xPLACEHOLDER' as `0x${string}`
